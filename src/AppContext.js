import { createContext } from 'react';

export const AppDashboardContext = createContext({});

export const AppDashboardContextProvider = AppDashboardContext.Provider;
export const AppDashboardContextConsumer = AppDashboardContext.Consumer;
